package _23_visitor._04_asyclic_visitor;

// A marker interface - it won't have any methods.
// The `visit` method in every interface extends this interface will be `optional`.
public interface Visitor {
}
