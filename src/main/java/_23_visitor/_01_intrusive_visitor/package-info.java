package _23_visitor._01_intrusive_visitor;

// We have the Expression class which has a lot of subclasses.
// We need to add the `print` functionality to it.
// Here, we will do it the "dirty" way, by adding the `print` method to the Expression class.